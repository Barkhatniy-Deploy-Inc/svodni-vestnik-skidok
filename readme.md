# 🪙 Сводный Вестник Скидок (Price Sentinel)

### *Telegram Mini App для благородного надзора за ценами*

Данное цифровое устройство предназначено для автоматического слежения за стоимостью товаров на различных торговых площадках. Оно избавляет пользователя от суеты ручного поиска, предоставляя наглядные графики и своевременные депеши о снижении цены. Идеально подходит для надзора за стоимостью запчастей для железных коней и прочих технических благ.

---

## 🏛 Архитектурное устройство

Система опирается на три столпа, за каждый из которых отвечает достойный муж из нашего триумвирата:

1.  **Лицевая сторона (Frontend):** Создана на **Vue.js 3**. Современный и отзывчивый интерфейс, встроенный непосредственно в Telegram Mini App.
2.  **Сердце системы (Backend):** Начертано на **Python** с использованием **FastAPI**. Обеспечивает асинхронную обработку запросов и сбор данных.
3.  **Хранилище (Database):** Надежная база данных **SQLite**, управляемая через **SQLAlchemy**. Вся летопись цен и реестры пользователей покоятся в строгом порядке.

---

## 🛠 Технологический стек

* **Backend:** `Python 3.10+` & `FastAPI` (Асинхронный сервер).
* **Database:** `SQLite` (Компактное хранение).
* **ORM:** `SQLAlchemy 2.0` (Декларативное управление таблицами).
* **Frontend:** `Vue.js 3` (Vite, Pinia для состояний).
* **Integration:** `Telegram WebApp SDK` (Связь с мессенджером).
* **Data Scraper:** `Httpx` & `BeautifulSoup4` (Незримые слуги-сборщики).

---

## 📋 Основные возможности

* **Надзор за ценами:** Вставка ссылки на товар и установка «желаемой цены» для уведомления.
* **Летопись перемен:** Автоматическое построение графиков стоимости (поддержка `Chart.js`).
* **Мгновенные депеши:** Уведомления от бота, как только цена в лавке достигнет указанного уровня.
* **Интерактивная панель:** Полноценное графическое приложение внутри чата, поддерживающее системные темы Telegram.

---

## 📂 Структура репозитория

```text
price-sentinel/
├── backend/            # Труды по логике и сбору данных
│   ├── app/
│   │   ├── api/        # Эндпоинты FastAPI
│   │   ├── core/       # Настройки безопасности и валидация initData
│   │   ├── models.py   # Схемы SQLAlchemy
│   │   ├── scraper.py  # Механизм обхода торговых лавок
│   │   └── database.py # Подключение к SQLite
│   └── main.py         # Точка входа сервера
├── frontend/           # Труды по интерфейсу (Vue.js)
│   ├── src/
│   │   ├── api/        # Связь с бэкендом (Axios)
│   │   ├── components/ # Графики и карточки товаров
│   │   └── App.vue     # Инициализация Telegram SDK
├── data/               # Место хранения файла базы данных (.sqlite)
└── README.md           # Сие описание
