# 📜 Отчет о разработке: Сводный Вестник Скидок (Price Sentinel)

Статус: **MVP**

---

## 🏛 Архитектура системы

Система построена на современном асинхронном стеке и разделена на два ключевых слоя.

### 1. Бэкенд (Сердце)
*   **Технологии:** Python 3.10+, FastAPI, SQLAlchemy 2.0.
*   **База данных:** SQLite (через `aiosqlite`).
*   **Парсинг:** `Httpx` + `BeautifulSoup4`. Специфическая логика для **Ozon, Wildberries, Яндекс Маркет**.
*   **Автоматизация:** `APScheduler` (проверка цен каждые 4 часа с рандомными паузами 3-10 сек).
*   **Уведомления:** Интеграция с Telegram Bot API.
*   **Безопасность:** Валидация `initData` от Telegram Mini App.

### 2. Фронтенд (Лицо)
*   **Технологии:** Vue.js 3 (Vite), Pinia.
*   **Визуализация:** `Chart.js` для графиков истории цен.
*   **Интеграция:** Telegram WebApp SDK для получения данных пользователя и стилизации под тему мессенджера.

---

## 📡 API Эндпоинты (v1)

| Метод | Путь | Описание | Ограничения |
| :--- | :--- | :--- | :--- |
| `GET` | `/api/v1/products/` | Список товаров пользователя | Требует Auth |
| `POST` | `/api/v1/products/` | Добавить товар (URL + цель) | Авто-парсинг при создании |
| `DELETE`| `/api/v1/products/{id}`| Удалить товар из надзора | - |
| `POST` | `/api/v1/products/update-all` | Ручное обновление всех цен | **Кулдаун 2 часа** |

---

## 🛠 Инструкция по запуску

### Режим разработки (Локально)
1.  **Бэкенд:**
    ```bash
    cd backend
    pip install -r requirements.txt
    python -m uvicorn main:app --reload
    ```
2.  **Фронтенд:**
    ```bash
    cd frontend
    npm install
    npm run dev
    ```

### Продакшн (Docker)
В папке `backend/` подготовлен `Dockerfile`.
```bash
docker build -t price-sentinel-back ./backend
docker run -p 8000:8000 --env-file .env price-sentinel-back
```

---

## 🧪 Тестирование
Проект покрыт автоматическими тестами:
*   **Backend:** `pytest` (API, Парсер, БД).
*   **Frontend:** `vitest` (Компоненты).

Запуск: `pytest backend/tests` и `cd frontend && npm run test`.

---

## 📝 Логирование
Все важные события сервера записываются в `backend/logs/app.log` с ротацией файлов.
